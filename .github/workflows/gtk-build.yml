name: GTK Build Validation

on:
  workflow_dispatch:
  push:
    paths:
      - "tools/build-gtk.sh"
      - "tools/prepare-hyde-live-smoke.sh"
      - "design/gtk/**"
      - "design/palette/**"
      - ".github/workflows/gtk-build.yml"

permissions:
  contents: read

jobs:
  build-gtk:
    name: Build Witcher3 GTK package
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes sassc xz-utils

      - name: Build and validate first GTK package
        run: ./tools/build-gtk.sh --package

      - name: Preserve first archive
        run: |
          set -Eeuo pipefail
          archive="Source/arcs/Gtk_Witcher3.tar.xz"
          test -f "$archive"
          cp "$archive" "$RUNNER_TEMP/Gtk_Witcher3.first.tar.xz"
          sha256sum "$archive" | tee "$RUNNER_TEMP/Gtk_Witcher3.first.sha256"

      - name: Build and validate second GTK package
        run: ./tools/build-gtk.sh --package

      - name: Verify byte-reproducible GTK archive
        run: |
          set -Eeuo pipefail
          archive="Source/arcs/Gtk_Witcher3.tar.xz"
          first="$RUNNER_TEMP/Gtk_Witcher3.first.tar.xz"

          first_sha="$(sha256sum "$first" | awk '{print $1}')"
          second_sha="$(sha256sum "$archive" | awk '{print $1}')"

          printf 'first SHA-256:  %s\n' "$first_sha"
          printf 'second SHA-256: %s\n' "$second_sha"

          test "$first_sha" = "$second_sha"
          cmp --silent "$first" "$archive"

          echo "GTK archive is byte-reproducible."

      - name: Prepare development HyDE smoke tree
        run: |
          set -Eeuo pipefail
          ./tools/prepare-hyde-live-smoke.sh --use-existing-gtk --output "$RUNNER_TEMP/hyde-live-smoke"

          staged="$RUNNER_TEMP/hyde-live-smoke"
          repo_theme="Configs/.config/hyde/themes/Witcher3/hypr.theme"
          staged_theme="$staged/Configs/.config/hyde/themes/Witcher3/hypr.theme"

          test -f "$staged/Source/arcs/Gtk_Witcher3.tar.xz"
          test -f "$staged/DEV_SMOKE_TEST.txt"
          grep -Fq '$ICON_THEME = Witcher3-HyDE' "$repo_theme"
          ! grep -Eq '^[[:space:]]*\$ICON_THEME[[:space:]]*=' "$staged_theme"
          grep -Eq '^[[:space:]]*\$GTK_THEME[[:space:]]*=[[:space:]]*Witcher3[[:space:]]*$' "$staged_theme"
          ! find "$staged" -type f -name 'Icon_*.tar.*' -print -quit | grep -q .
          ! find "$staged" -type f -name 'Cursor_*.tar.*' -print -quit | grep -q .

      - name: Inspect generated archive
        run: |
          set -Eeuo pipefail
          archive="Source/arcs/Gtk_Witcher3.tar.xz"
          test -f "$archive"
          echo "Archive size:"
          du -h "$archive"
          echo
          echo "SHA-256:"
          sha256sum "$archive"
          echo
          echo "Top-level archive entries:"
          tar -tJf "$archive" \
            | sed 's#^\./##' \
            | cut -d/ -f1 \
            | sort -u

      - name: Upload validated GTK package
        uses: actions/upload-artifact@v7
        with:
          name: Gtk_Witcher3
          path: Source/arcs/Gtk_Witcher3.tar.xz
          if-no-files-found: error
          retention-days: 7
