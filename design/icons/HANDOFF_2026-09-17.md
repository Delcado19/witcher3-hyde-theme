# Witcher3 Icon Artwork Handoff — 2026-09-18

## Authoritative repository state

Repository:

`Delcado19/witcher3-hyde-theme`

Current `main` HEAD before this documentation commit:

`db6b12d45ae341f7dd9c643dea6d180836ec4491` — `Merge complete Witcher3 MIME artwork`

MIME/Filetypes completion:

- PR #21 — `Complete Witcher3 MIME and filetype artwork`
- PR CI: Icon Build Validation #166 — green
- MIME/Filetypes: **85 / 85**
- merged W3-526–W3-610, preserving the existing W3-543 `application-pdf` pilot

Do not continue artwork on `icon-mimetypes-text-1`; its work is integrated.

## Artwork coverage on main

Current coverage: **394 / 645 canonical artwork identities**.

Complete groups:

- Actions/UI: **100 / 100**
- Places/Folders: **65 / 65**
- Status/Panel/Waybar: **95 / 95**
- Devices: **45 / 45**
- MIME/Filetypes: **85 / 85**

Artwork still incomplete:

- Applications: 3 accepted pilot icons currently present out of 220
- Categories/Misc: 1 accepted pilot icon currently present out of 35

Accepted pilot anchors that must not be recreated casually:

- Applications W3-070 `dolphin`
- Applications W3-082 `konsole`
- Applications W3-083 `kitty`
- Categories/Misc W3-621 `applications-system`

## Hybrid delivery decision — authoritative

The final icon package is **hybrid SVG/PNG**, not SVG-only.

Rules:

- use simplified SVG where additional raster detail has no visible benefit;
- use PNG where material, lighting, texture, or painted detail is visibly superior;
- one canonical may use SVG at smaller sizes and optimized PNG variants at larger sizes;
- raster-capable detailed artwork starts from a high-resolution master, initially around 1024×1024 or larger if visual testing justifies it;
- each emitted PNG size must be reviewed and may require size-specific optimization rather than blind downscaling;
- the SVG→PNG crossover is deliberately **not frozen yet**;
- determine that crossover visually through A/B comparison at representative sizes;
- do not use embedded-PNG-in-SVG as the default delivery mechanism;
- `style_class` controls visual/review language, not file format.

No delivery manifest exists yet by design. Add structured delivery metadata only if the crossover pilot shows that class/family rules are insufficient and per-canonical exceptions are required.

The current `tools/build-icons.py` scalable-only staging behavior remains a development scaffold and must be upgraded before final v1 packaging.

## Exact next work

Start Categories/Misc from **W3-611** on a fresh branch based on current `main`.

Use the matrix as the source of truth for canonical filenames.

Work in coherent subfamilies, update documentation after every completed family, and require green Icon Build Validation before starting the next family.

Do not recreate W3-621 `applications-system`; it already exists as the accepted Categories pilot anchor.

After Categories/Misc reaches **35 / 35**, merge it through one completion PR, update documentation again, and then return to Applications/Hero production and the unresolved SVG↔PNG crossover pilot.

## Categories/Misc art direction

Categories/Misc uses the **Emblem** class.

Keep:

- one strong category/tool metaphor per icon;
- coherent forged-metal, leather, parchment, crystal, gear, or control language;
- readable silhouettes at compact desktop sizes;
- restrained Witcher red;
- no copied vendor or Witcher game assets;
- no font-dependent text;
- no unnecessary micro-detail.

Mandatory Emblem review sizes:

- 22 px
- 32 px
- 48 px
- 64 px
- 128 px

Review surfaces:

- `#0A151E`
- `#171A1C`
- `#1C1813`
- `#262729`
- light edge case `#F2F0EA`

These review sizes do not define the future PNG/SVG crossover.

## Validation contract

Relevant checks:

- `python3 tools/validate-icon-sources.py`
- `python3 tools/build-icon-review.py --require-all`
- `python3 tools/build-icons.py`
- `.github/workflows/icon-build.yml`

The current release builder remains fail-closed until all 645 canonical artwork identities exist and the final hybrid delivery implementation replaces the scalable-only staging scaffold.

## Naming baseline

The canonical-name audit remains frozen:

- 645 unique canonical designs
- 206 aliases
- 0 canonical/alias namespace collisions

Pinned references remain:

- Freedesktop icon naming snapshot dated 2026-09-14
- `shared-mime-info 2.5.1`
- KDE Breeze `235730e69d90949621e4fee77fcc459772b7a8f0`
- repository application-identity snapshots

## New-chat resume instruction

If the user says **“weiter”** or **“go”**:

1. inspect current `main`;
2. confirm the MIME completion merge is present;
3. create or continue a fresh Categories/Misc artwork branch from the documented `main`;
4. continue directly at **W3-611** using matrix-authoritative canonical names;
5. update documentation after every completed subfamily;
6. require green icon CI before starting the next subfamily;
7. preserve the hybrid SVG/PNG decision and do not invent a crossover size before visual A/B review.
