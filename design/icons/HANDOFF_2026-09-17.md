# Icon Matrix Audit Handoff — 2026-09-18

## Branch

`icon-pilot-w3-409`

Verified branch state immediately before this handoff update:

`1198a8774bb392e516e155b4749a8b16632fb0b5` — `Align 3D model MIME canonicals`

Do **not** force-push over newer work. Re-check HEAD before writing because this branch has moved in parallel during earlier audit sessions.

## Completed / committed state

The sequential MIME/Filetypes naming audit is now complete through **W3-610**, which finishes the MIME/Filetypes design block.

The final validation run for `1198a8774bb392e516e155b4749a8b16632fb0b5` passed both:

- `Validate 645-design icon matrix`
- `Validate strict icon builder`

The matrix remains synchronized at 645 canonical designs with 206 aliases and no namespace collisions.

## Audio block — W3-589–W3-596

Resolved state:

- W3-589: `audio-generic` → `audio-x-generic`
  - Freedesktop standard generic audio MIME icon.
  - Matching pinned Breeze MIME asset exists.
- W3-590: `audio-mp3` → `audio-mpeg`
  - `shared-mime-info` primary MIME: `audio/mpeg`; `audio/mp3` is an alias.
- W3-591: retain `audio-flac`
  - Exact `audio/flac` primary MIME.
- W3-592: `audio-wav` → `audio-vnd.wave`
  - `audio/vnd.wave` is primary; `audio/wav` and `audio/x-wav` are aliases.
- W3-593: retain `audio-ogg`
  - Exact `audio/ogg` primary MIME.
- W3-594: `audio-opus` → `audio-x-opus+ogg`
  - Exact primary MIME in pinned `shared-mime-info 2.5.1`.
  - Lack of a dedicated pinned Breeze asset does not override MIME provenance.
- W3-595: `audio-m4a` → `audio-mp4`
  - `audio/mp4` is primary; `audio/m4a` is an alias.
- W3-596: retain `audio-playlist` as a validated `project-extension`
  - Generic playlist concept intentionally spans M3U, Apple MPEGURL, XSPF, WPL, and related playlist formats.
  - Do not narrow this design to one concrete playlist MIME type.

The Breeze Stage-C audit baseline was updated from 149 to 150 Freedesktop-resolved matrix names after W3-589 became `audio-x-generic`.

## Video block — W3-597–W3-603

Resolved state:

- W3-597: `video-generic` → `video-x-generic`
  - Freedesktop standard generic video MIME icon.
- W3-598: retain `video-mp4`
- W3-599: `video-mkv` → `video-matroska`
  - Primary MIME is `video/matroska`; `video/x-matroska` is an alias.
- W3-600: retain `video-webm`
- W3-601: `video-avi` → `video-vnd.avi`
  - Primary MIME is `video/vnd.avi`; `video/avi` and `video/x-msvideo` are aliases.
- W3-602: `video-mov` → `video-quicktime`
  - `*.mov` belongs to primary MIME `video/quicktime`.
- W3-603: retain `video-mpeg`

The Breeze Stage-C audit baseline is now **151** Freedesktop-resolved matrix names after W3-597 became `video-x-generic`.

## 3D / model block — W3-604–W3-610

Resolved state:

- W3-604: retain `model-3d` as a validated `project-extension`
  - No generic model MIME canonical exists in the pinned baseline.
  - Several model formats use `image-x-generic` as fallback, but that name is already owned by W3-577 and would erase the intended generic-3D distinction.
- W3-605: `model-blend` → `application-x-blender`
  - Exact Blender MIME identity in pinned `shared-mime-info`.
- W3-606: retain `model-stl`
- W3-607: retain `model-obj`
- W3-608: retain `model-fbx` as a validated `project-extension`
  - Pinned `shared-mime-info 2.5.1` contains no FBX MIME identity or `*.fbx` glob.
  - Pinned Breeze contains no FBX MIME canonical.
- W3-609: `model-glb` → `model-gltf-binary`
  - Exact MIME: `model/gltf-binary`.
- W3-610: `model-gltf` → `model-gltf+json`
  - Exact MIME: `model/gltf+json`.

The deliberate extensions for W3-596, W3-604, and W3-608 are recorded in:

`design/icons/standards/project-extension-review.csv`

## Audit tooling / pinned sources

Continue using the repository's pinned baselines rather than moving upstream targets:

- Freedesktop icon naming snapshot dated 2026-09-14
- `shared-mime-info 2.5.1` at the pinned commit prefix in CI
- KDE Breeze `235730e69d90949621e4fee77fcc459772b7a8f0`

Relevant tools:

- `tools/audit-freedesktop-icon-names.py`
- `tools/audit-shared-mime-info.py`
- `tools/audit-breeze-icon-names.py`
- `tools/validate-icon-matrix.py`
- `tools/normalize-icon-matrix.py`

## Next block: W3-611–W3-645 Categories/Misc

Resume sequentially at **W3-611 — `applications-accessories`**.

Procedure:

1. Re-check branch HEAD before any write.
2. Audit each canonical against the pinned Freedesktop snapshot first.
3. For unresolved generic desktop names, check the pinned Breeze namespace and semantic usage.
4. Use `project-extension` only when neither standard source provides a semantically equivalent canonical.
5. Keep CSV and Markdown matrix representations synchronized.
6. Record deliberate extensions in `design/icons/standards/project-extension-review.csv`.
7. Inspect each commit diff before moving the branch ref.
8. Do not start icon artwork production until the remaining naming audit is settled or an explicit decision changes the workstream.
