# Witcher3 Icon Artwork Handoff — 2026-09-18

## Repository / branch state

Repository:

`Delcado19/witcher3-hyde-theme`

Authoritative `main`:

`255ed260fb99dd6436c3e7b9071906573e779756`

Active branch:

`icon-hero-hybrid-pilot-1`

The branch is based on current `main`, is **ahead only** and contains the Hero hybrid-crossover tooling/documentation work. Do not restart this work from a new branch unless the branch has diverged unexpectedly.

## Coverage on main

Current canonical artwork coverage on `main`: **428 / 645**.

Complete groups:

- Actions/UI: **100 / 100**
- Places/Folders: **65 / 65**
- Status/Panel/Waybar: **95 / 95**
- Devices: **45 / 45**
- MIME/Filetypes: **85 / 85**
- Categories/Misc: **35 / 35**

Only Applications/Hero remains incomplete:

- Applications: **3 / 220** accepted pilot SVGs on `main`
- W3-070 `dolphin`
- W3-082 `konsole`
- W3-083 `kitty`

**217 Application/Hero canonicals remain.**

## Hybrid delivery decision — authoritative

The overall icon package may remain **hybrid SVG/PNG**, but Hero/Application delivery is now explicitly **fidelity-first**.

- Glyph and many Emblem families may use simplified SVG where simplification is visually appropriate;
- Hero SVG is optional and must look like the same approved high-detail artwork, not a flat/cartoon reinterpretation;
- Hero PNG is the visual source of truth for the current production-quality workflow;
- a Hero canonical may ship PNG-only at all approved sizes;
- there is no requirement to force SVG at small Hero sizes;
- if a practical PNG→SVG reconstruction passes visual fidelity and size/complexity checks, it may be used and then tested for a real crossover;
- if it does not pass, use PNG rather than lowering the Hero style bar;
- embedded-PNG-in-SVG does not count as successful vector delivery.

See `design/icons/HERO_STYLE_LOCK.md` for the authoritative rule.

The current `tools/build-icons.py` scalable-only staging behavior remains a development scaffold and must not dictate Hero artwork format.

## Critical correction — invalid procedural pilot

The procedural Dolphin/Kitty generators and their comparison outputs are **tooling fixtures only**.

They do not meet the production Hero quality bar and provide **zero visual evidence** for the crossover.

The earlier internal 96 px indication is **void**.

Never reintroduce that number as a presumed threshold unless a new, valid production-quality comparison independently establishes it.

## Hero style lock — 2026-09-18

The project owner has frozen the accepted high-detail forged-metal Dolphin treatment as the **Hero finish/style reference**.

Required consequences:

- dark blackened/weathered steel, worn silver, authored surface damage, dimensional lighting and controlled red accents define the target;
- flat/cartoon simplified Hero redraws are rejected;
- the rejected simplified Dolphin SVG must not be offered again;
- SVG is accepted for Hero only when it preserves subject, silhouette, composition, palette, depth and material language closely enough to look like the PNG artwork;
- otherwise the canonical is PNG-only;
- a visual/semantic PNG→SVG reconstruction and optimization tool may be developed to test whether a practical faithful SVG is possible.

The detailed rule is authoritative in `design/icons/HERO_STYLE_LOCK.md`.

## Approved Hero quality / identity references

### W3-070 Dolphin — current final direction

The project owner corrected Dolphin to a **circular forged medallion/seal** treatment.

Required visual character:

- large, immediately recognizable silver Dolphin emblem;
- blackened/weathered forged steel;
- engraved rune ring / structural ornament;
- authored scratches, abrasion and edge wear;
- controlled red recess/rim/emissive accents;
- convincing depth and material lighting;
- optional secondary scene/detail only if Dolphin identity remains dominant.

This corrected medallion direction is the **current final Dolphin identity reference**.

### W3-070 Dolphin — retired original folder concept

The original Dolphin concept was a layered folder/file-cabinet object with:

- steel plates;
- leather bindings;
- rivets;
- runes;
- large silver Dolphin emblem;
- heavy depth/material layering.

It is **retired as final Dolphin artwork**.

It may remain useful as a **detail-stress-test reference** because it is deliberately complex and can expose when raster material detail collapses during downscaling. It must be labelled as a stress test and must not replace the corrected Dolphin composition.

### W3-083 Kitty — current quality anchor

The project owner supplied a production-quality Kitty reference using:

- circular forged medallion;
- distinct cat/school head;
- terminal prompt element;
- weathered steel;
- rune ring;
- controlled red emissive accents.

Kitty establishes a second production-quality finish target.

**Important:** Dolphin and Kitty both being circular does not make a circular medallion the default Applications container. The 220 Hero icons still require varied silhouettes and constructions.

## Reference-image handling

The high-quality Dolphin/Kitty images supplied by the project owner in chat are **visual/art-direction references**.

Do not automatically commit those binary images to the repository unless provenance and redistribution rights are explicit.

Until then:

- document the visual decision in text;
- create/use project-owned or cleared production masters for release;
- do not treat unclear-provenance references as redistributable source assets.

## Valid Hero vector/crossover experiment

A Hero crossover candidate now requires **both** a production-quality raster master and a fidelity-passing vector reconstruction.

Required process:

1. start from the accepted high-detail PNG identity;
2. attempt a PNG→SVG reconstruction using visual segmentation, clean path fitting, layered gradients/masks/highlights and final SVG optimization;
3. compare SVG vs PNG first at 256 and 512 px;
4. reject the SVG immediately if subject/silhouette, composition, dominant colors, depth, lighting or material language are visibly different;
5. if it fails, record PNG-only for that candidate rather than inventing a crossover;
6. only if it passes, compare 32, 48, 64, 96, 128, 256 and 512 px at 100% native size on the standard five surfaces;
7. record SVG file size/complexity as part of acceptance;
8. repeat on a materially different Hero if a class-wide default is still desired.

The crossover criterion remains visible quality at normal viewing scale, but **fidelity to the same artwork comes first**.

## Tooling currently on the active branch

- `tools/build-hybrid-icon-review.py`
- `tests/test_hybrid_icon_review.py`
- `tools/generate-hybrid-pilot-dolphin.py` — tooling fixture only
- `tools/generate-hybrid-pilot-kitty.py` — tooling fixture only
- `design/icons/HYBRID_CROSSOVER_PILOT.md`
- CI wiring in `.github/workflows/icon-build.yml`

The review harness supports exact-size optimized PNG overrides and self-contained comparison output.

## Documentation discipline

After **every** subsequent meaningful step:

1. update README / pilot documentation / this handoff as applicable;
2. commit the step coherently;
3. push the active branch;
4. require green relevant CI before proceeding.

Do not claim a visual size review occurred unless actual production-quality renders were inspected.

## Conversation context / timely handover discipline

The working chat context must be treated as finite project state and monitored during long implementation sessions.

Rules:

- keep this handoff current after every meaningful completed step so a handover never depends on reconstructing many chat turns;
- re-check remaining conversation context periodically, especially before a large implementation/review phase;
- when roughly **25% or less** of the available context remains, begin a formal handover pass even if work could technically continue;
- when roughly **15% or less** remains, do not start a new major work phase before the handoff is refreshed and committed;
- perform an earlier handover if context compression, tool-state complexity, many uncommitted working artifacts, or an upcoming risky/large change makes continuation fragile;
- a handover must record at minimum: authoritative `main`, active branch and HEAD, CI state, accepted/rejected visual decisions, committed tooling/docs, uncommitted working assets and their status, exact next step, and anything that must explicitly **not** be resurrected;
- commit and push the refreshed handoff before moving to a new chat;
- do not rely on chat-only memory for a critical design or delivery decision that can be written into the repository.

The goal is a **deliberately early handover**, not emergency summarization at the context limit.

## Production-quality Hero master milestone — 2026-09-18

The first valid raster quality gate has been crossed in the working session.

- corrected high-detail Dolphin and Kitty Hero treatments were generated and visually accepted by the project owner as quality anchors;
- the accepted Dolphin treatment is the forged dark-steel / worn-silver / red-accent medallion now frozen by `HERO_STYLE_LOCK.md`;
- the attempted flat/simplified Dolphin SVG has been **rejected** and is removed from the active branch;
- that SVG did not preserve the approved subject/silhouette, color hierarchy or Witcher material finish and therefore provides zero crossover evidence;
- the next vector attempt must reconstruct the accepted PNG appearance itself;
- if that cannot be achieved at practical SVG complexity/size, PNG-only is the approved Hero outcome.

No Hero SVG has yet passed the fidelity gate and no SVG↔PNG crossover size is frozen.

## Exact next step

Do **not** resume broad Applications production yet.

Next:

1. use the accepted forged-medallion Dolphin raster identity as the PNG→SVG reconstruction target;
2. build a visually guided vector reconstruction/optimizer rather than a flat redraw or one-click trace;
3. optimize paths, reusable gradients/masks/filters and SVG structure;
4. compare the result against the PNG at 256 and 512 px first;
5. if it fails visual fidelity, record Dolphin as PNG-only and stop forcing SVG;
6. if it passes, run the seven-size native crossover comparison and measure SVG byte size/complexity;
7. update documentation immediately with the result.

## Resume instruction

If the user says **“weiter”** or **“go”**:

- inspect `main` and `icon-hero-hybrid-pilot-1`;
- confirm the branch is still ahead-only and CI-green;
- read this handoff, `design/icons/HERO_STYLE_LOCK.md`, and `design/icons/HYBRID_CROSSOVER_PILOT.md`;
- continue with the faithful Dolphin PNG→SVG reconstruction experiment;
- do not resurrect the rejected flat Dolphin SVG or the procedural 96 px result;
- accept PNG-only if the fidelity gate cannot be met practically;
- update documentation after the completed step.
