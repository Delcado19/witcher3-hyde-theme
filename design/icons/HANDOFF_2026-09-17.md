# Icon Matrix Audit Handoff — 2026-09-18

## Current repository state

Current integration target: `main`

Latest accepted artwork expansion:

`9a9411f4bccb83ca898815912254efd459bc266d` — `Merge core Actions UI icon families`

Historic naming-audit completion:

`ae6db8c58433ed4063fd758c26ada1ba9aaad788` — `Align Categories and Preferences icon canonicals`

Do **not** force-push over newer work. Re-check `main` before creating the next artwork branch.

## Naming audit status

The sequential canonical-name audit is now complete through **W3-645**.

Final validation for the completed matrix passed:

- `Validate 645-design icon matrix`
- `Validate strict icon builder`
- Freedesktop naming/context audit
- pinned `shared-mime-info 2.5.1` audit
- pinned KDE Breeze audit
- Flathub / Arch application identity audits
- Stage-G application provenance audit

The matrix remains:

- **645 unique canonical designs**
- **206 aliases**
- **0 canonical/alias namespace collisions**
- synchronized CSV and Markdown representations

## Final Categories/Misc block — W3-611–W3-645

The final block resolves to:

- **20 Freedesktop canonical matches**
- **12 additional exact Breeze canonical matches**
- **3 deliberate project extensions**

The three Categories/Misc project extensions are:

- W3-625 — `preferences-hardware`
  - generic hardware settings; no semantically equivalent Freedesktop/Breeze canonical
- W3-628 — `preferences-appearance`
  - broad appearance umbrella; theme/color/font/wallpaper canonicals are narrower
- W3-642 — `system-about`
  - system-information/about concept; distinct from the application `help-about` action

All three are recorded in:

`design/icons/standards/project-extension-review.csv`

### Canonical corrections in the final preference block

- W3-626: `preferences-network` → `preferences-system-network`
- W3-629: `preferences-keyboard` → `preferences-desktop-keyboard`
- W3-630: `preferences-mouse` → `preferences-desktop-mouse`
- W3-631: `preferences-display` → `preferences-desktop-display`
- W3-632: `preferences-audio` → `preferences-desktop-sound`
- W3-633: `preferences-bluetooth` → `preferences-system-bluetooth`
- W3-634: `preferences-power` → `preferences-system-power-management`
- W3-635: `preferences-notifications` → `preferences-desktop-notification`
- W3-636: `preferences-users` → `preferences-system-users`
- W3-637: `preferences-time` → `preferences-system-time`
- W3-638: `preferences-locale` → `preferences-desktop-locale`
- W3-639: `preferences-storage` → `preferences-system-disks`
- W3-640: `preferences-printer` → `preferences-devices-printer`

W3-629 and W3-638 are documented design-bucket exceptions because Freedesktop standardizes those exact names in the Applications context while the Witcher3 matrix groups the artwork under Categories/Misc.

The Breeze Stage-C audit baseline now expects **154 Freedesktop-resolved matrix names**.

## Previous completed MIME tail

The MIME/Filetypes audit is complete through W3-610.

Key final corrections include:

- W3-589: `audio-x-generic`
- W3-590: `audio-mpeg`
- W3-592: `audio-vnd.wave`
- W3-594: `audio-x-opus+ogg`
- W3-595: `audio-mp4`
- W3-597: `video-x-generic`
- W3-599: `video-matroska`
- W3-601: `video-vnd.avi`
- W3-602: `video-quicktime`
- W3-605: `application-x-blender`
- W3-609: `model-gltf-binary`
- W3-610: `model-gltf+json`

Deliberate tail extensions retained and documented:

- W3-596 — `audio-playlist`
- W3-604 — `model-3d`
- W3-608 — `model-fbx`

## Pinned naming sources

Continue using the repository's pinned baselines:

- Freedesktop icon naming snapshot dated 2026-09-14
- `shared-mime-info 2.5.1`
- KDE Breeze `235730e69d90949621e4fee77fcc459772b7a8f0`
- current repository application-identity snapshots

Relevant validation tools:

- `tools/validate-icon-matrix.py`
- `tools/normalize-icon-matrix.py`
- `tools/audit-freedesktop-icon-names.py`
- `tools/audit-shared-mime-info.py`
- `tools/audit-breeze-icon-names.py`
- application identity/provenance audit tools


## Pilot result — PASS — 2026-09-18

The complete 14-icon pilot is merged to `main` at:

`62d40a7c36dca6c059e492b77dc27eb59d067974` — `Merge complete 14-icon Witcher3 artwork pilot`

Final pilot state:

- 14 / 14 canonical pilot SVGs are present;
- every pilot SVG uses the preferred `0 0 1024 1024` authoring canvas;
- incremental source validation passes;
- `tools/build-icon-review.py --require-all` passes in CI;
- Glyph, Emblem, and Hero mandatory sizes were reviewed;
- all project dark surfaces plus a generic light edge-case surface were reviewed;
- light-surface contrast issues discovered in Audio/WLAN were corrected before merge;
- the Hero set remains silhouette-distinct and avoids a universal medallion/container rule;
- the pilot is accepted as the visual baseline.

## Artwork expansion status — 2026-09-18

The first family expansion is merged to `main` at:

`9a9411f4bccb83ca898815912254efd459bc266d` — `Merge core Actions UI icon families`

Current source coverage is **36 / 645 canonical SVGs**:

- 14 accepted pilot SVGs;
- 22 additional core Actions/UI Glyphs.

The complete P0 block **W3-221–W3-245** is now present:

- Document family W3-221–W3-226;
- Edit family W3-227–W3-235;
- Navigation family W3-236–W3-245.

All three family batches passed:

- incremental SVG source validation;
- permanent 14-icon pilot completeness gate;
- icon tooling fixture suite;
- release-artifact cleanliness checks.

Visual review was performed at 16/22/24/32/48 px and on the project dark surfaces plus a generic light edge-case. `edit-select-all` received the same restrained dark under-stroke strategy established during the pilot where light-surface separation required it.

## Next workstream: View / Zoom / Window / Tab Glyphs

Create a fresh branch from current `main` and continue sequentially at **W3-246**.

### View family

- W3-246 `view-refresh` — Circular swallow flight rune
- W3-247 `view-fullscreen` — Four expanding corner blades
- W3-248 `view-restore` — Four contracting corner blades
- W3-249 `view-grid` — Nine engraved stone tiles
- W3-250 `view-list-text` — Three parchment rows
- W3-251 `view-sort-ascending` — Ascending rune bars
- W3-252 `view-sort-descending` — Descending rune bars
- W3-253 `view-filter` — Alchemy funnel
- W3-254 `view-hidden` — Half-veiled eye medallion

### Zoom family

- W3-255 `zoom-in` — Lens with plus sigil
- W3-256 `zoom-out` — Lens with minus sigil
- W3-257 `zoom-fit-best` — Lens around framed landscape
- W3-258 `zoom-original` — Lens with 1:1 rune

Do not depend on font-rendered text for W3-258; construct the 1:1 mark as vector geometry.

### Window / Tab family

- W3-259 `window-close` — Crossed short blades
- W3-260 `window-minimize` — Low horizontal steel bar
- W3-261 `window-maximize` — Square steel frame
- W3-262 `window-restore` — Overlapping steel frames
- W3-263 `window-pin` — Dagger pinning parchment
- W3-264 `window-keep-above` — Crown over window plaque
- W3-265 `window-keep-below` — Window plaque under stone slab
- W3-266 `tab-new` — New parchment tab with plus
- W3-267 `tab-close` — Small tab with blade-cross

Hard constraints remain:

1. review Glyphs at 16/22/24/32/48 px;
2. preserve the accepted visual weight from W3-221–245;
3. use light-surface under-strokes only where needed, not universally;
4. keep red as a controlled accent;
5. commit coherent subfamilies separately and require green CI before the next subfamily;
6. do not jump to Status expansion until the core Actions/UI block is coherent.
