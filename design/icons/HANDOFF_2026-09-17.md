# Icon Matrix Audit Handoff — 2026-09-17

## Branch

`icon-pilot-w3-409`

Verified branch state immediately before this handoff note:

`01b81e627065fcfff173b8ce5cc15551bc102dbb` — `Align XCF MIME canonical with shared-mime-info`

Do **not** force-push over newer work. The branch moved in parallel during this audit, so always re-check HEAD before writing.

## Completed / committed state

The MIME/Filetypes audit has been reviewed through **W3-588**.

Current canonical names in the matrix include these verified corrections:

- W3-581: `image-svg` → `image-svg+xml`
- W3-583: `image-raw` → `image-x-dcraw`
- W3-586: `image-psd` → `image-vnd.adobe.photoshop`
- W3-587: `image-kra` → `application-x-krita`
- W3-588: `image-xcf` → `image-x-xcf`

The following image entries were reviewed and left unchanged in this pass:

- W3-578: `image-jpeg`
- W3-579: `image-png`
- W3-580: `image-webp`
- W3-582: `image-gif`
- W3-584: `image-heif`
- W3-585: `image-tiff`

The XCF correction was intentionally committed by itself after verifying that the diff changed only the CSV and Markdown matrix rows.

## Audit tooling already present

The branch contains audit helpers including:

- `tools/audit-shared-mime-info.py`
- `tools/audit-breeze-icon-names.py`

Use the pinned upstream/Breeze revisions already recorded by the repository rather than silently switching to moving upstream targets.

## Next block: W3-589–W3-596 Audio

Research was started but **no audio matrix changes were committed yet**. Resume from W3-589.

High-confidence pending corrections:

- W3-589: `audio-generic` → `audio-x-generic`
  - Freedesktop generic icon name confirmed.
  - Matching pinned Breeze MIME asset confirmed.
- W3-590: `audio-mp3` → `audio-mpeg`
  - `shared-mime-info` primary MIME is `audio/mpeg`; `audio/mp3` is an alias.
  - Matching pinned Breeze MIME asset confirmed.
- W3-592: `audio-wav` → `audio-vnd.wave`
  - `shared-mime-info` primary MIME is `audio/vnd.wave`; `audio/wav` / `audio/x-wav` are aliases.
  - Matching pinned Breeze MIME asset confirmed.
- W3-595: `audio-m4a` → `audio-mp4`
  - `shared-mime-info` primary MIME is `audio/mp4`; `audio/m4a` is an alias.
  - Matching pinned Breeze MIME asset confirmed.

Pending but requires one final decision/check before committing:

- W3-594: current `audio-opus`
  - `shared-mime-info` points to `audio/x-opus+ogg`, implying icon name `audio-x-opus+ogg`.
  - No dedicated matching 64 px pinned Breeze asset was found under `audio-opus` or `audio-x-opus+ogg` during the pass.
  - Prefer MIME provenance over inventing an extension-style name, but re-check before write.
- W3-596: current `audio-playlist`
  - No same-named pinned Breeze MIME asset was found.
  - Do not arbitrarily narrow the generic playlist concept to M3U/PLS without deciding whether the matrix slot is meant to be a generic concept or a concrete MIME type.

Preliminary review indicated W3-591 `audio-flac` and W3-593 `audio-ogg` are likely already correct, but re-run the final upstream/Breeze check together with the rest of the audio block before committing.

## Resume procedure

1. Re-check branch HEAD; do not assume `01b81e6` is still current.
2. Continue sequentially at **W3-589**.
3. Validate each proposed canonical name against `shared-mime-info` first, then the pinned Breeze tree where applicable.
4. Keep CSV and Markdown matrix files synchronized.
5. Before moving the branch ref, inspect the commit diff and ensure only intended rows/files changed.
6. Continue after audio with W3-597 onward only after W3-589–596 is fully settled.
