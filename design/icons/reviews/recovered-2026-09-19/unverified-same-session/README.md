# Same-session recovered candidates

These files are exact recovered bytes from the Witcher3-HyDE icon-generation session, but prior approval/canonical status has not been established.

They are preserved here to prevent loss and must not be treated as approved masters unless the handoff/history explicitly proves approval.

- `power-symbol.png`: recovered from persistent Library transfer `power-icon.png.b64`.
- `dolphin-folder-emblem.png`: recovered from persistent Library transfer `dolphin-folder-emblem.png.b64`.

Approved/final artwork remains in the normal approved/recovered directories.
